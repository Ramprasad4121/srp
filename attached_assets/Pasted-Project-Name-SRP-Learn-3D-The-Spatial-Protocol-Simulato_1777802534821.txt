Project Name: SRP Learn 3D - The Spatial Protocol SimulatorRole: You are a swarm of 500 specialized engineers (Design, Frontend, Blockchain, and AI) tasked with building a 3D, immersive Web3 learning platform.Core Objective: Build a system that translates raw smart contract code (Solidity/Rust) into a real-time, interactive 3D simulation where an AI Avatar guides the user through the "physical" execution of the code.1. The Environment (The World-Building Agents):Create a dark, cyberpunk-themed "Neural Void" using React Three Fiber and Three.js.Design 3D assets (using Spline/GLTF) for:The Vault: A translucent, glowing container representing Contract State.Data Streams: Moving particles representing transaction flow.Logic Gates: Physical barriers representing require() or if/else statements.2. The Translation Layer (The Logic Agents):Develop a "Code-to-Visual Parser." When a user inputs code, the AI must output a sequence of "Visual Keyframes."Example: If the code is balances[msg.sender] -= amount, the agent must trigger an animation where the "Vault" volume decreases and the "User's" glow increases.3. The AI Avatar System (The Mentor Agents):Integrate a 3D Ready Player Me avatar with lip-sync (Convai/Rhubarb).The avatar must have "Spatial Awareness." If a bug occurs on line 14, the avatar must physically point at the glowing "Broken Logic Gate" in the 3D scene and explain the fix.4. Multi-Chain Physics (The Protocol Agents):EVM Mode: Implement a sequential execution visual where transactions move through a single "Execution Tunnel."Solana Mode: Implement a parallel execution visual where multiple "Data Lanes" process transactions simultaneously to demonstrate Sealevel.5. Feedback Loop:Every user action must trigger a Haptic/Visual feedback. If they write efficient code, the world becomes "bright/stable." If the code has a vulnerability (e.g., Reentrancy), the 3D world should "glitch" and show a "Shadow Attacker" stealing crystals.How to Distribute Your 500 AgentsTo ensure this build is perfect, divide your swarm into these squads:SquadAgent CountPrimary TaskArchitects50Defining the 3D metaphors (e.g., What does a "Flash Loan" look like physically?).GL-Engineers150Writing the Three.js shaders and physics for the 3D environment.Protocol Experts100Building the logic parsers for Solidity (EVM) and Rust (Solana).AI Integration100Training the Avatar's "Socratic Twin" logic to be a helpful, witty tutor.QA/Security100Creating the Adversarial Simulations (the glitches/attacks that teach security).The "Deep" Technical GoalThe final product should be a "State-Sync Engine." When a transaction is sent on a testnet, the 3D world mirrors the state change in real-time. The user shouldn't look at a block explorer; they should look at the "World" and see the value moving.